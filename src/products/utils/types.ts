export type CreateProductParams = {
    title: string;
    description: string;
    price: number;
}

export type UpdateProductParams = {
    title: string;
    description: string;
    price: number;
}